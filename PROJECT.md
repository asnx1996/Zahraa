# موقع كوتش زهراء — سياق المشروع

## نبذة

موقع لمدربة لياقة تنشر عليه **كورسات رياضية** و**وصفات أكل** فيها السعرات
والماكروز والمكونات وطريقة العمل ورابط فيديو. عربي بالكامل، RTL، موبايل أولاً.

## التقنيات (لا تغيّرها)

* **Vanilla HTML/CSS/JS** بملف واحد لكل صفحة — بدون React، بدون build tools، بدون npm
* **الاستضافة:** GitHub Pages → https://zahraa-hashim.github.io/Zahraa/
* **الباك اند:** Supabase (قاعدة بيانات + مصادقة + تخزين صور)
* كل الروابط الداخلية **نسبية** (`admin.html` مو `/Zahraa/admin.html`) لأن الموقع تحت
مجلد فرعي وممكن ينتقل لدومين خاص لاحقاً
* مكتبة Supabase محفوظة بالريبو تحت `vendor/` — **مو من CDN**. السكربت هذا
  يشوف كلمة السر بصفحة اللوحة، فما ننطيه لطرف ثالث. للتحديث: نزّل النسخة
  الجديدة بـ `vendor/` وبدّل المسار بالصفحتين

## Supabase

```
SUPABASE\_URL  = https://kjpwcvhsjfuwzketkjkk.supabase.co
sb\_publishable\_7Sq6BeRa9KQPR1zy1VFHFg\_WOhUoUt0 = <الصق هنا الـ publishable key اللي يبدي بـ sb\_publishable\_...>
```

> الـ publishable key آمن ينحط بالكود العام. الحماية الحقيقية من الـ RLS.
> ⚠️ مفتاح `sb\_secret\_...` ممنوع منعاً باتاً يدخل الكود أو الريبو.

### الجداول

**`recipes`** — id (uuid) · title · cover · tag (فطور/غداء/عشاء/سناك) · duration ·
servings (int) · kcal (int) · protein · carbs · fat (numeric) · video ·
ingredients (jsonb) · steps (jsonb) · is\_published (bool) · sort\_order (int) ·
created\_at · updated\_at

**`courses`** — id (uuid) · title · cover · tag (مبتدئ/متوسط/متقدم) · area ·
duration · video · description · moves (jsonb) · is\_published · sort\_order ·
created\_at · updated\_at

**`site\_info`** — صف واحد فقط (id=1) · name · name\_en · initial · tagline ·
tagline\_en · bio · bio\_en · avatar\_url · instagram · whatsapp ·
**ومحتوى الصفحة الرئيسية:** hero\_eyebrow · hero\_title · hero\_sub (jsonb ثنائي اللغة) ·
hero\_image · about\_image (نص) · stats · creds · plans · quotes · faq · credit ·
**sections** (jsonb)

**`admins`** — user\_id (uuid، مفتاح خارجي لـ auth.users) · note · created\_at.
مين يكدر يعدّل. شوف قسم الصلاحيات تحت.

كل حقل نصّي ثنائي اللغة مخزون كـ `{"ar":"…","en":"…"}`. شوف `schema.sql` لشكل كل حقل.

### قواعد الأمان بالكود — لا تكسرها

* **ماكو `onerror` مكتوب بالوسوم.** أي صورة تفشل تاخذ `.failed` من مستمع واحد
  بمرحلة الالتقاط داخل `wire()` بالصفحتين. إذا ضفت صورة جديدة ما تحتاج تسوي شي.
* **`safeUrl()`** — أي رابط يجي من الداتابيس ويروح لـ `href` لازم يمر بيها.
  `esc()` تمنع الخروج من الوسم بس ما تمنع `javascript:`.
* **`heroHtml()`** — `hero\_title` هو **الحقل الوحيد** اللي ينكتب بـ `innerHTML`،
  ومسموح بيه وسم واحد: `<span class="hl">`. أي شي غيره ينطلع نص عادي.
  باقي الحقول كلها `textContent` أو `esc()`.
* **سياسة CSP** بوسم `<meta>` بالصفحتين. حدودها مكتوبة بالتعليق فوقها:
  `script-src` بيها `'unsafe-inline'` لأن الكود داخل الملف، و`frame-ancestors`
  ما تشتغل بوسم meta. إذا ضفت مصدر خارجي جديد (صور، خطوط، API) لازم تضيفه
  للسياسة بالصفحتين وإلا ينحجب بصمت.

### إظهار وإخفاء أقسام الصفحة

عمود `sections` يقرر أي قسم يبين بالصفحة الرئيسية:

```json
{"trust":true,"plans":false,"about":true,"reviews":true,"faq":true,"peek":true,"cta":true}
```

المفاتيح السبعة: `trust` (شريط المميزات) · `plans` · `about` · `reviews` ·
`faq` · `peek` (أحدث الوصفات والكورسات) · `cta` (الشريط الأخير).
**المفتاح الناقص معناه ظاهر** — يعني عمود فاضي = كل شي يبين، وما ينخفي إلا
اللي تطفينه صراحة.

> مفتاح `calc` انشال لأن حاسبة السعرات انحذفت. لو باقي بعمود `sections`
> بالداتابيس فهو مهمل، ما يأثر على شي.

القسم المطفي يختفي هو وروابط التنقل اللي تشير له (الهيدر والفوتر)، والمحتوى
مالته يبقى محفوظ بالداتابيس.

> ⚠️ **قائمة المفاتيح مكررة بثلاث أماكن ولازم تبقى متطابقة:**
> `SECTION\_NODES` بـ `index.html` (يربط المفتاح بعناصر الصفحة) ·
> `SECTION\_KEYS` بـ `admin.html` (يبني المفاتيح باللوحة) · وتعليق `schema.sql`.
> إذا ضفت قسم جديد، ضيف له `id` بالماركب و`data-navfor` لروابطه.

### شكل حقول الـ jsonb

```json
ingredients: \[{"q":300,"u":"غم","n":"صدر دجاج","alt":\["تونة","توفو"]}]
steps:       \["الخطوة الأولى","الخطوة الثانية"]
moves:       \[{"n":"بلانك","s":"3 × 40 ثانية"}]
```

> `alt` **اختياري** — بدائل المكوّن. ما ينكتب أصلاً إذا فاضي، والوصفات
> القديمة تشتغل بدونه. ينتكتب باللوحة مفصول بفاصلة، ويظهر بالموقع سطر
> صغير تحت اسم المكوّن. تنبحث بعد — اللي تدوّر «تونة» تلگي الوصفة
> اللي تنفع بيها تونة بدل الدجاج.

### الصلاحيات (RLS — مطبّقة أصلاً، لا تعدّلها)

* `anon` → قراءة الصفوف اللي `is\_published = true` فقط
* **المشرفة** → كل العمليات (إضافة/تعديل/حذف) + رؤية المخفي
* التسجيل الذاتي **مطفي** — حساب المدربة انسوّى يدوياً من الداشبورد

> ⚠️ «المشرفة» تعني **حساب مسجّل بجدول `admins`**، مو أي حساب مسجّل دخول.
> الفرق مهم: حتى لو انفتح التسجيل الذاتي يوم من الأيام، الحساب الجديد ما
> يكدر يعدّل ولا يحذف. الفحص كله يمر بدالة `public.is\_admin()`.
>
> لإضافة مشرفة: `insert into public.admins (user_id) values ('<uuid>');`

### تخزين الصور

Bucket عام اسمه **`media`**. الرفع للمشرفة فقط، القراءة للجميع.

**قيود البكت (مطبّقة على السيرفر، مو بس بالمتصفح):** الحجم الأقصى ٥ ميغا،
والأنواع المسموحة `jpeg` / `png` / `webp` فقط، والرفع محصور بالمجلدات الأربعة
تحت. القيم هاي مكررة بـ `admin.html` (`OK\_TYPES` و `MAX\_BYTES`) حتى تطلع رسالة
خطأ عربية واضحة بدل رفض مبهم من السيرفر — **إذا غيّرتها بمكان، غيّرها بالثنين**.
بعد الرفع نخزن الـ public URL بعمود `cover` (أو `avatar_url`).

**تنظيم المجلدات** — كل صورة تروح لمجلد حسب مكان رفعها، ونافذة الاختيار تفتح
على مجلد السياق تلقائياً (وصفة ← `recipes`، كورس ← `courses`، محتوى الصفحة ← `site`):

| المجلد | شنو يخزن | وين تنرفع منه |
|-|-|-|
| `media/recipes/` | أغلفة الوصفات | فورم الوصفة |
| `media/courses/` | أغلفة الكورسات | فورم الكورس |
| `media/site/` | صور الواجهة المتحركة وصورة «عن الكوتش» | تبويب محتوى الصفحة |
| `media/avatar/` | الصورة الشخصية | تبويب معلومات الموقع |

الصور تنضغط بالمتصفح قبل الرفع (عرض ١٤٠٠px، جودة ٠.٨٢) — صورة موبايل ٦ ميغا
تصير ~٢٠٠ كيلو.

**وين تظهر كل صورة:**

* `avatar_url` → شعار الهيدر والفوتر، محل الحرف المختصر
* `hero_images` → سلايدر الواجهة (يتقلب كل ٥ ثواني)
* `about_image` → قسم «عن الكوتش»
* `cover` → بطاقة الوصفة/الكورس ورأس نافذة التفاصيل

اسم الملف ينتولد تلقائياً: `<timestamp>-<random>.<ext>` — يعني ما تنطبّق صورة على
صورة أبداً حتى لو نفس الاسم الأصلي.

تبويب **الصور** باللوحة يعرض كل صور البكت مع المجلد، الحجم، التاريخ، و**وين مستعملة
كل صورة** (اسم الوصفة/الكورس). الحذف من التبويب يحذف من التخزين نهائياً، ويحذّرج إذا
الصورة مستعملة بمكان.

> ⚠️ تبديل صورة عنصر ما يحذف الصورة القديمة من البكت — تبقى بالمكتبة لحد ما تحذفينها
> يدوياً من تبويب الصور. هيچي تكدرين ترجعينها إذا غيّرتي رأيج.

## الملفات

|الملف|الحالة|
|-|-|
|`index.html`|✅ كل المحتوى يجي من Supabase — ماكو أي بيانات ثابتة بالكود|
|`admin.html`|✅ دخول Supabase، ٥ تبويبات (وصفات / كورسات / صور / محتوى الصفحة / معلومات الموقع) — مظبوطة للموبايل|
|`schema.sql`|✅ انفّذ على Supabase، والبيانات الحالية مزروعة بالجداول|
|`hardening.sql`|✅ تحديث القاعدة الشغّالة: عمود `sections` + تحصين الصلاحيات (آمن يتكرر)|
|`views.sql`|⚠️ **لازم ينفّذ على Supabase** — جدول `item_views` ودالة `bump_view` (آمن يتكرر). بدونه عدّاد المشاهدات ما يشتغل، وباقي الموقع يشتغل عادي|
|`vendor/supabase-2.58.0.js`|✅ مكتبة Supabase محفوظة محلياً بدل CDN|
|`sw.js`|✅ service worker — كاش للخطوط والصور، وأبداً ما يكشّ طلبات Supabase|
|`manifest.webmanifest`|✅ يخلي الموقع ينثبّت على الشاشة الرئيسية|
|`icon-192.png` / `icon-512.png`|✅ أيقونات التطبيق|

## تبويبات الموقع الثلاثة

الموقع كله **صفحة وحدة** (`index.html`)، بس مقسّم لثلاث تبويبات بشريط لازك
تحت الهيدر. التبديل يخفي أقسام ويظهر غيرها — بدون تحميل صفحة ولا نزول للأسفل.

| التبويب | شنو يعرض |
|-|-|
| **تعريف بالمدربة** | الواجهة · الباقات · عن الكوتش · الآراء · الأسئلة · **أحدث الوصفات والكورسات** · شريط الدعوة |
| **الأكل** | **عدّاد اليوم** · الكتالوج بوضع الوصفات |
| **الكورسات** | نفس الكتالوج بوضع الكورسات |

المصدر: ثابت `PAGES` بـ `index.html`. لإضافة قسم لتبويب، ضيف الـ selector
مالته للمصفوفة — و`ALL\_PAGE\_SECTIONS` تنبني منها لوحدها.

> ⚠️ **آليتان للإخفاء ولازم يبقن منفصلات:**
> خاصية `hidden` = تبديل التبويبات · كلاس `.sec-hidden` = قرار المدربة من
> اللوحة. لو استعملوا نفس الآلية، كل تبديل تبويب چان يمسح إعدادات الإخفاء.

### الروابط

| الرابط | يودّي لـ |
|-|-|
| `./` أو `#/` | تبويب التعريف |
| `#/recipes` · `#/courses` | تبويب الأكل · الكورسات |
| `#/r/<id>` · `#/c/<id>` | التبويب المناسب + نافذة التفاصيل |
| `#/cart` | قائمة التسوّق |
| `#plans` · `#faq` · `#about` · `#calc` · `#catalog` | روابط قديمة منشورة — الموجّه ينقلها لتبويبها ويمرّر لها |

الموجّه `openFromHash()` ينتادى من ثلاث جهات: الإقلاع، و`popstate` (زر الرجوع)،
و`hashchange`. **الثلاثة ضروريين** — الانتقال لهاش جديد يطلّع `hashchange` مو
`popstate`، وزر الرجوع بالعكس.

## مميزات الصفحة الرئيسية

* **عدد الحصص يمشي وياه كلشي** — لما الزائرة تزيد الحصص بنافذة التفاصيل،
  تتغيّر **السعرات والماكروز** وياها مو بس المكونات، ويطلع سطر صغير يذكّرها
  بقيمة الحصة الوحدة. والعدد اللي تختاره ينمشي وياه للسلة ولعدّاد اليوم عبر
  خاصية `data-n` على الزرين.

  > ⚠️ **الأرقام بالداتابيس للحصة الوحدة، والمكونات للوصفة كاملة.**
  > فالماكروز تنضرب بـ **عدد الحصص** (`n`)، والمكونات تنضرب بـ **معامل
  > التوسيع** (`n / r.servings`). خلط الاثنين كان بقّة حقيقية: مجموع السلة
  > چان يطلع نص القيمة الصحيحة.

* **قائمة التسوّق** — كل وصفة بيها ستيبر يغيّر كميتها (٢ من هاي، ٣ من ذيچ)،
  والمكونات والمجاميع تتحدّث فوراً. تنجمع وتوحّد المتكرر، **وتنقسم على أقسام
  السوق** (خضار وفواكه · لحوم وأسماك · ألبان وبيض · حبوب ومعلبات · بهارات
  وزيوت · متفرقات) حتى المشي بالسوق يصير بمسار واحد، وتنطبع بورقة مستقلة (`#printArea`)
  فيها مربعات فارغة تنعلّم بالقلم بالسوق، وصفوف متبادلة اللون، ومجموع
  السعرات والماكروز بالأسفل. تنسخ كذلك كنص.

  > **التصنيف ينستنتج من اسم المكوّن** بجدول كلمات (`MARKET`) — ماكو حقل
  > بالداتابيس ولا شغل زايد على المدربة، ويشتغل بأثر رجعي على كل الوصفات
  > المكتوبة من زمان. اللي ما ينعرف يروح لـ«متفرقات».
  >
  > ⚠️ ترتيب `MARKET` مهم: «فلفل اسود» لازم تنمسك كبهار قبل ما «فلفل»
  > تمسكها كخضروات. والمطابقة على مستوى **الكلمة** مو النص كله —
  > بدونها «فاصوليا بيضاء» چانت تصير ألبان لأن بيها «بيض». الكلمات
  > القصيرة (رز، خس، بيض) تطابق الكلمة كاملة، والطويلة يكفي جزء منها.

  > الورقة تنبنى بـ `buildPrintSheet()` وتنعرض بـ `body.printing` تحت
  > `@media print` فقط — لأن القائمة نفسها مرسومة جوّا الشيت، والشيت مخفي
  > بالطباعة. ألوان الورقة **صريحة مو متغيّرات CSS** حتى الوضع الليلي
  > ما يطبع خلفية سوداء.

* **لوحة الفلترة والترتيب** — زر واحد بشريط الأدوات يفتح لوحة فيها كل شي.
  قبلها چان صف فلاتر ثاني دائم ياكل نص الشاشة بالموبايل قبل أول بطاقة.

  * **الترتيب** (`SORTS`): ترتيب الكوتش (`sort_order` — الافتراضي) · الأحدث ·
    الأعلى بروتين · الأقل سعرات · الأسرع تحضيراً. الأخيرتان للوصفات بس.
  * **فلاتر سريعة** للوصفات: بروتين عالي ≥٣٠غم · سعرات قليلة ≤٤٠٠ ·
    كارب قليل ≤٢٥غم · سريعة التحضير ≤١٥ دقيقة — العتبات بثابت `MACRO\_FILTERS`
  * **المنطقة** للكورسات (عمود `area`)، وما تبين إذا كل الكورسات بنفس المنطقة
  * **المحفوظة فقط**

  اللوحة تنرسم جوّا `#sheet` نفسه (`VIEW='filter'`) — تاخذ السحب للإغلاق
  وحصر التركيز وزر الرجوع بدون تكرار كود. الشارة على الزر تكول شكد فلتر
  شغّال، وزر «شوفي النتائج» يعرض العدد ويتحدّث مع كل ضغطة.

  ### ⚠️ اللوحة **ما تتمرر ككل** — لا تشيل هالبنية

  الشيت ياخذ كلاس `.pane` لمن `VIEW==='filter'`، فيصير عمود ثابت:

  ```
  .sheet-inner.pane   overflow:hidden · flex column   ← ارتفاعه من max-height
    .pane-head        flex:none                        ← رأس مثبّت
    .pane-scroll      flex:1 · overflow-y:auto         ← الوسط، يتمرر إذا اضطر
    .pane-foot        flex:none                        ← «مسح» + «شوفي النتائج»
  ```

  قبلها چان الشيت كله يتمرر، والنتيجة اهتزاز مستمر ما تكدر تختار بيه:
  المحتوى أطول من الشاشة ⇒ يطلع شريط تمرير ⇒ يقص ١٥px من العرض ⇒
  الشرائح (`.flt-opts`) تعيد التفاف سطورها ⇒ الارتفاع يتغيّر ⇒ الشريط
  يختفي ⇒ العرض يرجع ⇒ … حلقة ما توقف.

  البنية هاي تقطع الحلقة **من جذرها**: ارتفاع الصندوق يجي من `max-height`
  مو من المحتوى، فمهما تغيّر المحتوى ما يأثر على العرض. حتى لو الوسط
  تمرر (شاشة قصيرة)، الصندوق الخارجي ثابت. وزر «شوفي النتائج» يبقى
  بالشاشة دائماً بدل ما تنزلين تدوّرين عليه.

  > `scrollbar-gutter:auto` على `.pane` مقصودة — اللوحة ما تتمرر فحجز
  > مكان الشريط عليها ياكل ١٥px بلا سبب. الحجز ينتقل لـ `.pane-scroll`.

  > المحتوى انضغط بعد حتى ما يوصل الحافة أصلاً: انشال السطر التوضيحي
  > تحت العنوان، وسطر شرح المحفوظة، وقسم «المحفوظة» كله ما يبين إذا
  > ماكو ولا محفوظة، والذيل صار صف واحد بدل زرّين فوك بعض.
  > النتيجة: ٥١٩px بالموبايل و٤٢٨px بالديسكتوب — بعيدة عن السقف.

  > ⚠️ ترتيب المصدر مهم: قواعد `.pane-*` لازم تبقى **بعد** `.sheet-body`
  > بالستايل شيت. نفس الوزن بالتحديد، فاللي بعدين يغلب — لو انتقلت فوك،
  > `padding-bottom:40px` مال `.sheet-body` يرجع يشتغل والذيل ينقص.

  > تبديل التبويب يصفّر الترتيب والفلاتر — مفاتيح الوصفات (سعرات/بروتين)
  > ماكو معنى إلها بالكورسات، ولو بقت شغّالة تصير فرز صامت بحقل فاضي.

* **المفضّلة** — قلب على كل بطاقة وزر بنافذة التفاصيل، وفلتر «المحفوظة فقط».
  تنحفظ بـ `localStorage` تحت `cz-fav`.

  > ⚠️ **محلية بالجهاز بالكامل.** ماكو حسابات ولا داتابيس — مفضّلة كل زائرة
  > تخصّها هي وما تشوفها ولا وحدة ثانية، وما تنتقل وياها لو فتحت الموقع من
  > تلفون ثاني. مفضّلة مشتركة تحتاج تسجيل دخول للزائرات وجدول جديد وRLS.

* **وصفات وكورسات مشابهة** — ٣ بطاقات صغيرة بأسفل نافذة التفاصيل.
  الترتيب بنقاط: نفس التصنيف ٤٠ · قرب السعرات لحد ٣٠ (بالوصفات) أو نفس
  المنطقة ٣٠ (بالكورسات) · تقاطع المكوّنات لحد ٢٠. الضغط يبدّل محتوى
  الشيت بمكانه (`replaceState`) بدل ما يكدّس شيت فوك شيت.

* **أحدث الوصفات والكورسات** بتبويب التعريف — ٣ بطاقات من كل نوع مع زر يودّي
  للتبويب. الضغط على بطاقة ينقل للتبويب ويفتح التفاصيل بنفس الوقت.

* **بيانات منظّمة (JSON-LD)** — `injectStructuredData()` تبني `Recipe` لكل وصفة
  (بالسعرات والماكروز والمكونات والخطوات) و`Course` لكل كورس، وتحطهن بوسم
  `#ldjson` بالـ head. المدة تنحوّل لصيغة ISO (`٣٠ دقيقة` → `PT30M`).

* **انتقالات ناعمة** — تبديل التبويب وفتح التفاصيل يمرّون بـ View Transitions:
  صورة البطاقة تتمدد لرأس نافذة التفاصيل. تنطفي تلقائياً إذا المتصفح ما يدعمها
  أو المستخدمة مطفّية الحركة.

  > ⚠️ وعود `startViewTransition` **ترفض** إذا انلغى الانتقال (تبويب مخفي مثلاً)،
  > فلازم `catch` على `ready` و`finished` وإلا يطلع unhandled rejection.
  > و`view-transition-name` لازم ينشال بعد الانتهاء وإلا الانتقال الجاي ينكسر.

* **مشاركة أصلية** — `navigator.share` بالموبايل، ونسخ الرابط بالديسكتوب.

* **قائمة تسوّق مجمّعة** — الزائرة تعلّم وصفات متعددة، والموقع يجمع المكونات ويوحّد
  المتكرر (٣٠٠غم دجاج + ٢٠٠غم = ٥٠٠غم)، مع مجموع السعرات والماكروز.
  تنحفظ بـ `localStorage` تحت `cz-cart`، وتنرسل واتساب أو تنتسخ.

  الماكروز: بروتين ١.٨غم/كغم، دهون ٢٥٪ من السعرات، والباقي كارب.
  زر الواتساب يجهّز رسالة فيها الأرقام.
* **روابط مباشرة** — `#/r/<id>` للوصفة و `#/c/<id>` للكورس و `#/cart` للقائمة.
  فتح الرابط يفتح التفاصيل مباشرة. زر «نسخ الرابط» داخل التفاصيل.
* **سلايدر الواجهة** — صور `hero_images` تتقلب كل ٥ ثواني بتلاشي ناعم وحركة تقريب خفيفة،
  مع نقاط تنقل. يوقف عند مرور الماوس، وعند إخفاء التبويب، ومع `prefers-reduced-motion`.
  صورة وحدة = ثابتة بدون نقاط.
* **تبديل اللغة** — تلاشي ٢٢٠ملي قبل التبديل حتى ما يصير قفز مفاجئ عند انقلاب الاتجاه،
  ومكان التمرير ينثبّت. يتخطّى التأثير إذا المستخدم مطفّي الحركة من النظام.

## ⚠️ فخ الشيت: اهتزاز شريط التمرير

`.sheet-inner` يوصل `max-height` ويصير فيه تمرير. إذا انشال أي واحد من
السطرين هذولا يرجع الاهتزاز:

```css
scrollbar-gutter:stable;   /* المحور العمودي */
overflow-x:hidden;         /* المحور الأفقي */
```

**الآلية** — حلقة تغذية راجعة بين الارتفاع والعرض:

* **عمودياً:** ظهور الشريط يقصّ ١٥px من العرض ⇒ الشرائح اللي تلتف
  (`.flt-opts`) تعيد ترتيب سطورها ⇒ الارتفاع يتغيّر ⇒ الشريط يختفي ⇒
  العرض يرجع ⇒ … ٦٠ مرة بالثانية
* **أفقياً:** المحتوى يفيض ٣px بالكسور (حجز الشريط + `width:100%` بالـ RTL)
  ⇒ يطلع شريط أفقي يزيد ١٥px على الارتفاع ⇒ الفيض يروح ⇒ يختفي ⇒ …

النتيجة اللي تشوفها: اللوحة تهتز والأزرار تهرب من تحت المؤشر فما تنضغط.

> ⚠️ **ولا تستبدل `innerHTML` عند كل ضغطة داخل اللوحة.** الزر اللي تحت
> المؤشر ينهدم وينبني من جديد فالضغطة الثانية تروح على عنصر مو موجود،
> والتمرير يرجع للأعلى فاللي يختار من نص اللوحة ينقذف للراس.
> `syncFilterPanel()` تحدّث `aria-pressed` وعدد النتائج بمكانهم — وهذا
> كل اللي يتغيّر فعلاً.

## البحث

البحث ينسخ كل نصوص العنصر بكومة وحدة (`haystack()`) — العنوان والتصنيف
والمنطقة والمدة والوصف والمكوّنات وبدائلها والخطوات والتمارين — ويطلب
**كل كلمة** بالبحث تكون موجودة بيها (AND).

> قبلها چان يقارن جملة البحث كاملة بكل حقل لحاله، فـ«دجاج مشوي» ما تلگي
> وصفة عنوانها «صدر دجاج» وبمكوّناتها «مشوي» — الكلمتين بحقلين مختلفين.
> هسه كل كلمة زيادة تضيّق النتيجة، والترتيب بين الكلمات ما يفرق.

الكومة تنحسب مرة وحدة لكل عنصر وتنخزن بـ `x._hay`. والتطبيع (`norm()`)
يتجاهل الهمزات والتاء المربوطة والتشكيل.

## عدّاد المشاهدات

جدول `item_views` منفصل + دالة `bump_view` — شوف `views.sql`.

**ليش جدول منفصل مو عمود بـ `recipes`؟** لأن أي `update` على `recipes`
يشغّل ترايكر `touch_updated_at` ويخلي كل فتحة وصفة تبين «تعديل». وبعدين
`anon` ما عنده صلاحية `update` على `recipes` أصلاً ولا نريد ننطيه إياها.

* الزيادة تمر بدالة `security definer` — ماكو صلاحية كتابة مباشرة لأي أحد
* الدالة ما تزيد إلا إذا العنصر **موجود ومنشور** — بدونها أي واحد ينفخ
  الجدول بمعرّفات وهمية
* الموقع يمنع التكرار داخل نفس الجلسة (`sessionStorage` تحت `cz-seen`)
* القراءة للمشرفة فقط — الرقم يبين بصف اللوحة

> ⚠️ الرقم «كم مرة انفتحت التفاصيل»، مو عدد أشخاص. للمقارنة بين الوصفات
> يكفي ويزيد؛ لو انراد رقم يُعتمد عليه بالإعلان، لازم تحديد معدّل على
> مستوى السيرفر (Edge Function + IP) — ومو موجود هسه.

> إذا `views.sql` ما انفّذ، الطلب يفشل بصمت والصفحتين تشتغلن عادي —
> بس العدّاد ما يبين (`VIEWS_ON` بـ `admin.html`).

## إعادة الترتيب باللوحة

مقبض سحب بكل صف بدل زرّي فوك/تحت. السحب بالماوس وباللمس، والأسهم
(↑/↓) للكيبورد — الاثنين ينتهون لـ `applyOrder(ids)`.

* `applyOrder` **يعيد ترقيم القائمة كلها ١..ن** ويرسل الصفوف اللي تغيّر
  رقمها فقط — بدل تبديل رقمين بين جارين. هيچي السحب لمسافة بعيدة يشتغل،
  والأرقام تبقى بلا فجوات ولا تكرار
* الشاشة تتحدّث أول والحفظ وراها، وترجع لحالتها إذا فشل الطلب
* وسط السحب **ما ننقل عناصر الـ DOM** — الصف المسحوب يتحرك بـ `transform`
  والباقي ينزاح بمقدار ارتفاعه. تحريك الـ DOM يغيّر مواقع كل شي ويخلي
  الحساب يلاحق نفسه
* العتبة: **حافة** الصف المسحوب تعبر **مركز** جاره (يعني «غطّيت نصّه فخذ
  مكانه»). المقارنة بالمراكز وحدها تغلط بخطوة كاملة
* المراكز تنقاس مرة وحدة قبل السحب — الصفوف مو بنفس الارتفاع (عنوان طويل
  ينكسر سطرين)، فالقسمة على ارتفاع واحد تزحف بالقوائم الطويلة

> ⚠️ **المقبض ينطفي لمن يكون البحث شغّال.** الترتيب ينحسب على القائمة
> الكاملة، ولو سحبتي وسط نتائج مفلترة راح ينعاد ترقيم العناصر المخفية غلط.

## ⚠️ حدود مشاركة الروابط

`og:image` بالـ `<head>` **ثابت** ويشير لصورة الواجهة. لما ينشارك رابط وصفة معينة
بالواتساب أو الفيسبوك، تطلع صورة الواجهة مو صورة الوصفة — لأن زواحف المشاركة
ما تشغّل جافاسكربت، وGitHub Pages ما يقدر يولّد HTML لكل وصفة.

> إذا انراد صورة مختلفة لكل وصفة، لازم استضافة تدعم SSR (Netlify / Vercel / Cloudflare)
> بدل GitHub Pages.

**مهم:** إذا بدّلتي صورة الواجهة من تبويب «محتوى الصفحة»، لازم تبدّلين يدوياً
قيمة `content` بوسم `<meta property="og:image">` بـ `index.html` حتى تتطابق.

## ما ينحفظ بجهاز الزائرة

كل هذا **محلي بالجهاز** — ماكو ولا واحد منهم يوصل الداتابيس ولا يبين
لزائرة ثانية.

| المفتاح | شنو يخزن | متى ينمسح |
|-|-|-|
| `cz-cart` | قائمة التسوّق `[{id,servings}]` | لمن تمسحها الزائرة |
| `cz-day` | أكل اليوم `{d,items}` | تلقائياً بيوم جديد |
| `cz-fav` | المفضّلة `[id]` | لمن تشيلها الزائرة |
| `cz-lang` · `cz-theme` | اللغة والوضع الليلي | — |
| `cz-seen` (sessionStorage) | العناصر اللي انحسبت مشاهدتها | بنهاية الجلسة |

## اللغات

الموقع واللوحة الثنين عربي/إنكليزي مع زر تبديل بالهيدر، والاختيار ينحفظ بـ
`localStorage` تحت المفتاح `cz-lang`. الاتجاه (`dir`) يتبدل تلقائياً بين `rtl` و `ltr`.

* نصوص الواجهة كلها مترجمة من قاموس `L` بـ `index.html` و `T` بـ `admin.html`
* التصنيفات (فطور/غداء/مبتدئ/…) تنخزن بالعربي بالداتابيس وتنعرض مترجمة عبر دالة `tg()`
* **محتوى الوصفات والكورسات نفسه (العناوين، المكونات، الخطوات) يبقى باللغة اللي انكتب بيها**
  — لأن الجداول ما بيها أعمدة إنكليزية. إذا انراد محتوى مترجم لازم تنضاف أعمدة
  `title_en` / `steps_en` وهكذا.

## المطلوب — بالترتيب

### ١. بناء `admin.html`

* تسجيل دخول بإيميل/باسورد (Supabase Auth) + زر خروج
* إذا مو مسجّل دخول: ما يظهر غير فورم الدخول
* تبويبين: الوصفات / الكورسات + قسم ثالث لتعديل `site\_info`
* جدول يعرض كل العناصر مع أزرار تعديل/حذف/نشر-إخفاء
* فورم إضافة وتعديل يغطي كل الأعمدة، مع:

  * محرر ديناميكي للمكونات (إضافة/حذف سطر: كمية + وحدة + اسم)
  * محرر ديناميكي للخطوات وللتمارين
  * **رفع صورة** لبكت `media` وتعبئة `cover` تلقائياً + معاينة الصورة
* تأكيد قبل الحذف
* نفس هوية التصميم مال `index.html` (نفس متغيرات الـ CSS والخطوط)

### ٢. تحويل `index.html` ليقرا من Supabase

* شيل المصفوفات الثابتة، وجيب البيانات بـ `select` مرتّبة بـ `sort\_order`
* اعرض حالة تحميل (skeleton) وحالة خطأ مفهومة إذا فشل الاتصال
* `site\_info` تتعبى منها الهيدر والبايو ولنكات الانستغرام والواتساب

## هوية التصميم (لا تكسرها)

```
--paper:#F6F5F2  --surface:#FFFFFF  --ink:#14201B  --soft:#63736C
--brand:#C2410C  --pine:#0E7C66  --amber:#E9A227  --clay:#D1495B
خطوط: Cairo (عناوين، --f-head) · Noto Sans Arabic (نص، --f-body) · IBM Plex Mono (أرقام، --f-num)
```

> الخطوط تنستدعى دائماً عبر المتغيرات `--f-head` / `--f-body` / `--f-num` — لا تكتب اسم
> الخط مباشرة بأي قاعدة CSS حتى يبقى التبديل من مكان واحد.

العنصر المميز بالموقع هو **شريط الماكروز** الملوّن (بروتين أخضر / كارب كهرماني /
دهون برتقالي) — يبقى كما هو.

