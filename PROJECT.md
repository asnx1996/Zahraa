# موقع كوتش زهراء — سياق المشروع

## نبذة

موقع لمدربة لياقة تنشر عليه **كورسات رياضية** و**وصفات أكل** فيها السعرات
والماكروز والمكونات وطريقة العمل ورابط فيديو. عربي بالكامل، RTL، موبايل أولاً.

## التقنيات (لا تغيّرها)

* **Vanilla HTML/CSS/JS** بملف واحد لكل صفحة — بدون React، بدون build tools، بدون npm
* **الاستضافة:** GitHub Pages → https://asnx1996.github.io/Zahraa/
* **الباك اند:** Supabase (قاعدة بيانات + مصادقة + تخزين صور)
* كل الروابط الداخلية **نسبية** (`admin.html` مو `/Zahraa/admin.html`) لأن الموقع تحت
مجلد فرعي وممكن ينتقل لدومين خاص لاحقاً
* مكتبة Supabase تنجاب من CDN مباشرة (بدون تثبيت)

## Supabase

```
SUPABASE\_URL  = https://kjpwcvhsjfuwzketkjkk.supabase.co
sb\_publishable\_7Sq6BeRa9KQPR1zy1VFHFg\_WOhUoUt0 = <الصق هنا الـ publishable key اللي يبدي بـ sb\_publishable\_...>
```

> الـ publishable key آمن ينحط بالكود العام. الحماية الحقيقية من الـ RLS.
> ⚠️ مفتاح `sb\_secret\_...` ممنوع منعاً باتاً يدخل الكود أو الريبو.

### الجداول

**`recipes`** — id (uuid) · title · cover · tag (فطور/غداء/عشاء/سناك) · duration ·
servings (int) · kcal (int) · protein · carbs · fat (numeric) · video ·
ingredients (jsonb) · steps (jsonb) · is\_published (bool) · sort\_order (int) ·
created\_at · updated\_at

**`courses`** — id (uuid) · title · cover · tag (مبتدئ/متوسط/متقدم) · area ·
duration · video · description · moves (jsonb) · is\_published · sort\_order ·
created\_at · updated\_at

**`site\_info`** — صف واحد فقط (id=1) · name · name\_en · initial · tagline ·
tagline\_en · bio · bio\_en · avatar\_url · instagram · whatsapp ·
**ومحتوى الصفحة الرئيسية:** hero\_eyebrow · hero\_title · hero\_sub (jsonb ثنائي اللغة) ·
hero\_image · about\_image (نص) · stats · creds · plans · quotes · faq · credit (jsonb)

كل حقل نصّي ثنائي اللغة مخزون كـ `{"ar":"…","en":"…"}`. شوف `schema.sql` لشكل كل حقل.

### شكل حقول الـ jsonb

```json
ingredients: \[{"q":300,"u":"غم","n":"صدر دجاج"}]
steps:       \["الخطوة الأولى","الخطوة الثانية"]
moves:       \[{"n":"بلانك","s":"3 × 40 ثانية"}]
```

### الصلاحيات (RLS — مطبّقة أصلاً، لا تعدّلها)

* `anon` → قراءة الصفوف اللي `is\_published = true` فقط
* `authenticated` → كل العمليات (إضافة/تعديل/حذف)
* التسجيل الذاتي **مطفي** — حساب المدربة انسوّى يدوياً من الداشبورد

### تخزين الصور

Bucket عام اسمه **`media`**. الرفع للمسجّل دخوله فقط، القراءة للجميع.
بعد الرفع نخزن الـ public URL بعمود `cover` (أو `avatar_url`).

**تنظيم المجلدات** — كل صورة تروح لمجلد حسب مكان رفعها، ونافذة الاختيار تفتح
على مجلد السياق تلقائياً (وصفة ← `recipes`، كورس ← `courses`، محتوى الصفحة ← `site`):

| المجلد | شنو يخزن | وين تنرفع منه |
|-|-|-|
| `media/recipes/` | أغلفة الوصفات | فورم الوصفة |
| `media/courses/` | أغلفة الكورسات | فورم الكورس |
| `media/site/` | صور الواجهة المتحركة وصورة «عن الكوتش» | تبويب محتوى الصفحة |
| `media/avatar/` | الصورة الشخصية | تبويب معلومات الموقع |

الصور تنضغط بالمتصفح قبل الرفع (عرض ١٤٠٠px، جودة ٠.٨٢) — صورة موبايل ٦ ميغا
تصير ~٢٠٠ كيلو.

**وين تظهر كل صورة:**

* `avatar_url` → شعار الهيدر والفوتر، محل الحرف المختصر
* `hero_images` → سلايدر الواجهة (يتقلب كل ٥ ثواني)
* `about_image` → قسم «عن الكوتش»
* `cover` → بطاقة الوصفة/الكورس ورأس نافذة التفاصيل

اسم الملف ينتولد تلقائياً: `<timestamp>-<random>.<ext>` — يعني ما تنطبّق صورة على
صورة أبداً حتى لو نفس الاسم الأصلي.

تبويب **الصور** باللوحة يعرض كل صور البكت مع المجلد، الحجم، التاريخ، و**وين مستعملة
كل صورة** (اسم الوصفة/الكورس). الحذف من التبويب يحذف من التخزين نهائياً، ويحذّرج إذا
الصورة مستعملة بمكان.

> ⚠️ تبديل صورة عنصر ما يحذف الصورة القديمة من البكت — تبقى بالمكتبة لحد ما تحذفينها
> يدوياً من تبويب الصور. هيچي تكدرين ترجعينها إذا غيّرتي رأيج.

## الملفات

|الملف|الحالة|
|-|-|
|`index.html`|✅ كل المحتوى يجي من Supabase — ماكو أي بيانات ثابتة بالكود|
|`admin.html`|✅ دخول Supabase، ٥ تبويبات (وصفات / كورسات / صور / محتوى الصفحة / معلومات الموقع)|
|`schema.sql`|✅ انفّذ على Supabase، والبيانات الحالية مزروعة بالجداول|
|`sw.js`|✅ service worker — كاش للخطوط والصور، وأبداً ما يكشّ طلبات Supabase|
|`manifest.webmanifest`|✅ يخلي الموقع ينثبّت على الشاشة الرئيسية|
|`icon-192.png` / `icon-512.png`|✅ أيقونات التطبيق|

## مميزات الصفحة الرئيسية

* **قائمة تسوّق مجمّعة** — الزائرة تعلّم وصفات متعددة، والموقع يجمع المكونات ويوحّد
  المتكرر (٣٠٠غم دجاج + ٢٠٠غم = ٥٠٠غم)، مع مجموع السعرات والماكروز.
  تنحفظ بـ `localStorage` تحت `cz-cart`، وتنرسل واتساب أو تنتسخ.
* **حاسبة سعرات** (`#calc`) — معادلة Mifflin-St Jeor + مضاعف النشاط + الهدف.
  الماكروز: بروتين ١.٨غم/كغم، دهون ٢٥٪ من السعرات، والباقي كارب.
  زر الواتساب يجهّز رسالة فيها الأرقام.
* **روابط مباشرة** — `#/r/<id>` للوصفة و `#/c/<id>` للكورس و `#/cart` للقائمة.
  فتح الرابط يفتح التفاصيل مباشرة. زر «نسخ الرابط» داخل التفاصيل.
* **سلايدر الواجهة** — صور `hero_images` تتقلب كل ٥ ثواني بتلاشي ناعم وحركة تقريب خفيفة،
  مع نقاط تنقل. يوقف عند مرور الماوس، وعند إخفاء التبويب، ومع `prefers-reduced-motion`.
  صورة وحدة = ثابتة بدون نقاط.
* **تبديل اللغة** — تلاشي ٢٢٠ملي قبل التبديل حتى ما يصير قفز مفاجئ عند انقلاب الاتجاه،
  ومكان التمرير ينثبّت. يتخطّى التأثير إذا المستخدم مطفّي الحركة من النظام.

## ⚠️ حدود مشاركة الروابط

`og:image` بالـ `<head>` **ثابت** ويشير لصورة الواجهة. لما ينشارك رابط وصفة معينة
بالواتساب أو الفيسبوك، تطلع صورة الواجهة مو صورة الوصفة — لأن زواحف المشاركة
ما تشغّل جافاسكربت، وGitHub Pages ما يقدر يولّد HTML لكل وصفة.

> إذا انراد صورة مختلفة لكل وصفة، لازم استضافة تدعم SSR (Netlify / Vercel / Cloudflare)
> بدل GitHub Pages.

**مهم:** إذا بدّلتي صورة الواجهة من تبويب «محتوى الصفحة»، لازم تبدّلين يدوياً
قيمة `content` بوسم `<meta property="og:image">` بـ `index.html` حتى تتطابق.

## اللغات

الموقع واللوحة الثنين عربي/إنكليزي مع زر تبديل بالهيدر، والاختيار ينحفظ بـ
`localStorage` تحت المفتاح `cz-lang`. الاتجاه (`dir`) يتبدل تلقائياً بين `rtl` و `ltr`.

* نصوص الواجهة كلها مترجمة من قاموس `L` بـ `index.html` و `T` بـ `admin.html`
* التصنيفات (فطور/غداء/مبتدئ/…) تنخزن بالعربي بالداتابيس وتنعرض مترجمة عبر دالة `tg()`
* **محتوى الوصفات والكورسات نفسه (العناوين، المكونات، الخطوات) يبقى باللغة اللي انكتب بيها**
  — لأن الجداول ما بيها أعمدة إنكليزية. إذا انراد محتوى مترجم لازم تنضاف أعمدة
  `title_en` / `steps_en` وهكذا.

## المطلوب — بالترتيب

### ١. بناء `admin.html`

* تسجيل دخول بإيميل/باسورد (Supabase Auth) + زر خروج
* إذا مو مسجّل دخول: ما يظهر غير فورم الدخول
* تبويبين: الوصفات / الكورسات + قسم ثالث لتعديل `site\_info`
* جدول يعرض كل العناصر مع أزرار تعديل/حذف/نشر-إخفاء
* فورم إضافة وتعديل يغطي كل الأعمدة، مع:

  * محرر ديناميكي للمكونات (إضافة/حذف سطر: كمية + وحدة + اسم)
  * محرر ديناميكي للخطوات وللتمارين
  * **رفع صورة** لبكت `media` وتعبئة `cover` تلقائياً + معاينة الصورة
* تأكيد قبل الحذف
* نفس هوية التصميم مال `index.html` (نفس متغيرات الـ CSS والخطوط)

### ٢. تحويل `index.html` ليقرا من Supabase

* شيل المصفوفات الثابتة، وجيب البيانات بـ `select` مرتّبة بـ `sort\_order`
* اعرض حالة تحميل (skeleton) وحالة خطأ مفهومة إذا فشل الاتصال
* `site\_info` تتعبى منها الهيدر والبايو ولنكات الانستغرام والواتساب

## هوية التصميم (لا تكسرها)

```
--paper:#F6F5F2  --surface:#FFFFFF  --ink:#14201B  --soft:#63736C
--brand:#C2410C  --pine:#0E7C66  --amber:#E9A227  --clay:#D1495B
خطوط: Cairo (عناوين، --f-head) · Noto Sans Arabic (نص، --f-body) · IBM Plex Mono (أرقام، --f-num)
```

> الخطوط تنستدعى دائماً عبر المتغيرات `--f-head` / `--f-body` / `--f-num` — لا تكتب اسم
> الخط مباشرة بأي قاعدة CSS حتى يبقى التبديل من مكان واحد.

العنصر المميز بالموقع هو **شريط الماكروز** الملوّن (بروتين أخضر / كارب كهرماني /
دهون برتقالي) — يبقى كما هو.

